registry=https://registry.npmjs.org/
hoist-pattern[]=*happy-dom*
# Security: Prevent automatic script execution during install
# Can be overridden with --ignore-scripts=false if needed
ignore-scripts=true
